description = "⏱️ Найти провисания в монтаже — детектор скуки"

prompt = """
Используй инструмент aim_extract_pacing для анализа ритма монтажа.

Входные данные: {{args}}

Нужен один из параметров:
- videoPath — абсолютный путь к локальному видеофайлу
- url — ссылка на видео

Опционально: slowThresholdSec — порог "провисания" в секундах (по умолчанию 4 сек).

Выведи:
- Таймкоды всех смен кадра
- Выделенные провисания (где зритель засыпает) с конкретными рекомендациями по ускорению
- Общую оценку динамики монтажа
"""
