description = "🪝 Проанализировать и усилить хук видео (первые 5 секунд)"

prompt = """
Используй инструмент aim_analyze_hook для оценки и усиления хука видео.

Входные данные: {{args}}

Нужен один из параметров:
- videoPath — абсолютный путь к локальному видеофайлу
- url — ссылка на видео

Анализ сфокусирован только на первых 5 секундах. 
Выдай 5 конкретных усиленных вариантов хука с объяснением какой триггер используется в каждом.

Если ничего не указано — попроси пользователя предоставить путь или ссылку.
"""
